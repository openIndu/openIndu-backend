# Database

Owner: `backend` agent (with `data` agent for production SQL).
Purpose: DB design, ER models, DDL, table analysis.
Artifacts: `database-design.md`, `er-model.md`, `*.sql`.
Migrations: see repo root `alembic/` (Alembic) — this dir is for design docs, not migration scripts.
